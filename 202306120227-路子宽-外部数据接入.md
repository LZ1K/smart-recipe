# Smart Recipe 智能菜谱助手 — 外部数据接入设计

**学号-姓名**：202306120227-路子宽
**课程**：智能应用开发
**项目**：Smart Recipe 智能菜谱助手

---

## 一、数据源

### 1.1 天气数据（wttr.in — 免费开放API）

| 项目 | 说明 |
|------|------|
| **名称** | wttr.in 全球天气 |
| **接入方式** | HTTP GET，无需注册/API Key |
| **调用地址** | `https://wttr.in/{城市}?format=j1` |
| **关键字段** | temp_C（气温）、humidity（湿度）、weatherDesc（天气状况）、windspeedKmph（风速） |
| **更新策略** | 每次对话/刷新聊天页时拉取，结果存入 `WeatherRecord` 表持久化 |

### 1.2 季节性食材数据（自建模拟数据）

| 项目 | 说明 |
|------|------|
| **名称** | 中国当季食材参考价格 |
| **接入方式** | 运行时脚本生成（`src/lib/seasonal-data.ts`），存入 PostgreSQL |
| **数据来源** | 全国主要城市农贸批发市场月度均价（教学演示），基于 2024-2025 同期价格趋势推算 |
| **关键字段** | name（食材名）、price（元/斤）、month（月份）、season（季节） |
| **更新策略** | 每月自动刷新；首次运行 `npx tsx src/lib/seed-seasonal.ts` 预填充 |

---

## 二、数据流设计

```
┌──────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  wttr.in API │────▶│ 四级降级获取策略   │────▶│ WeatherRecord   │
│  (外部天气)   │     │ API→缓存→推算→默认 │     │ (PostgreSQL)    │
└──────────────┘     └──────────────────┘     └────────┬────────┘
                                                       │
┌──────────────┐     ┌──────────────────┐              │
│ 季节性数据模拟 │────▶│ SeasonalIngredient│─────────────┤
│  (本地生成)   │     │ (PostgreSQL)      │              │
└──────────────┘     └──────────────────┘              │
                                                       ▼
                                              ┌─────────────────┐
                                              │  异常检测引擎    │
                                              │  L2: 温度/湿度/  │
                                              │  价格异常 → 通知  │
                                              └────────┬────────┘
                                                       │
                                                       ▼
                                              ┌─────────────────┐
                                              │  建议调整引擎    │
                                              │  L3: 标签加权/   │
                                              │  分类优先 → UI   │
                                              └────────┬────────┘
                                                       │
                                                       ▼
                                              ┌─────────────────┐
                                              │  聊天页侧边栏    │
                                              │  WeatherCard 组件│
                                              └─────────────────┘
```

### 四级降级数据获取策略（加分项）

| 级别 | 方案 | 场景 | 数据标记 |
|------|------|------|----------|
| L1 | wttr.in API 实时调用 | 网络正常 | `source: "api"` |
| L2 | 读取本地 DB 24h 内缓存 | API 超时/无网络 | `source: "cache"` |
| L3 | 季节推算（根据当前月份估计气温） | 缓存过期 | `source: "seasonal_estimate"` |
| L4 | 默认值（温和天气） | 所有方案失败 | — |

---

## 三、L2：异常检测与主动通知

### 规则 1：极端温度检测（高温/低温）

| 条件 | 级别 | 响应 |
|------|------|------|
| `temp ≥ 38°C` | danger | 强烈建议推荐凉拌菜、冷面、绿豆汤，避免火锅红烧 |
| `32°C ≤ temp < 38°C` | warning | 推荐清淡解暑菜品 |
| `temp ≤ 0°C` | danger | 强烈建议推荐火锅、炖菜、热汤，避免凉拌冷面 |
| `0°C < temp ≤ 10°C` | warning | 多推荐热汤炖菜 |

### 规则 2：高湿度食材保存预警

| 条件 | 响应 |
|------|------|
| `humidity ≥ 70%` | 提醒用户注意干货密封保存、绿叶菜尽快食用 |

### 规则 3：食材价格异常检测

| 条件 | 类型 | 响应 |
|------|------|------|
| `同比上月价格涨幅 > 30%` | price_spike | 推荐替代食材，提醒合理囤货 |
| `同比上月价格跌幅 > 25%` | price_drop | 推荐含该食材的菜谱（当季供应充足，性价比较高） |

### 测试案例（5条）

| # | 数据类型 | 测试用例 | 预期结果 |
|---|----------|----------|----------|
| 1 | 正常 | 气温 25°C，湿度 50% | 无预警，正常推荐 |
| 2 | 异常-高温 | 气温 39°C | danger 级别高温预警 |
| 3 | 异常-低温 | 气温 -5°C | danger 级别低温预警 |
| 4 | 边界 | 气温 32°C，湿度 69% | warning 高温预警，湿度无预警（边界） |
| 5 | 异常-价格 | 上月价格 10，本月 35 | price_spike 预警，建议替代食材 |

---

## 四、L3：数据驱动的建议调整

### 规则 1：天气 → 菜谱标签加权

```
高温(≥32°C) → boostedTags: [清淡, 凉拌, 快手, 低脂]
           → suppressedTags: [暖身, 冬季, 辣的]
低温(≤10°C) → boostedTags: [暖身, 冬季, 炖煮, 下饭]
           → suppressedTags: [凉拌, 清淡, 冷菜]
高湿(≥70%) → boostedTags: [汤羹, 暖身, 微辣]
```

### 规则 2：季节 → 食材与分类优先

```
春季(3-5月) → 当季食材: 春笋/韭菜/菠菜/荠菜/豌豆
           → 优先推荐: 素菜、家常菜
夏季(6-8月) → 当季食材: 冬瓜/苦瓜/黄瓜/番茄/丝瓜
           → 优先推荐: 素菜、汤羹
秋季(9-11月) → 当季食材: 莲藕/山药/南瓜/螃蟹/板栗
            → 优先推荐: 汤羹、荤菜
冬季(12-2月) → 当季食材: 白菜/萝卜/羊肉/山药
            → 优先推荐: 荤菜、汤羹、家常菜
```

### 验证效果

| 规则 | 输入 | 输出 | 验证方式 |
|------|------|------|----------|
| 天气调整 | ≥32°C（例：39°C） | boostedTags 含"清淡""快手"，suppressedTags 含"暖身""辣的" | API 返回 JSON |
| 季节调整 | 5月 | recommendedCategories: ["素菜","家常菜"] | API 返回 JSON |
| 价格异常 | 5月 | 黄瓜番茄等显示价格涨跌预警 | API 返回 JSON |

---

## 五、核心代码清单

| 文件 | 作用 |
|------|------|
| `prisma/schema.prisma` | 新增 `WeatherRecord` + `SeasonalIngredient` 模型 |
| `src/lib/weather.ts` | 天气获取（四级降级）+ L2 异常检测 + L3 建议调整 |
| `src/lib/seasonal-data.ts` | 12个月当季食材数据 + L2 价格异常检测 + L3 季节推荐 |
| `src/lib/seed-seasonal.ts` | 季节数据初始化脚本 |
| `src/app/api/external-data/weather/route.ts` | 天气数据 API（聚合异常+调整） |
| `src/app/api/external-data/seasonal/route.ts` | 季节数据 API（聚合食材+价格+推荐） |
| `src/components/ui/weather-card.tsx` | 聊天页侧边栏天气卡片（展示+预警+调整建议） |
| `src/components/ui/seasonal-card.tsx` | 聊天页侧边栏季节食材卡片（价格+推荐+价格预警） |
| `src/lib/seed-price-alerts.ts` | 价格预警测试数据脚本（注入上月对比数据） |

---

## 六、加分项自查

| 加分项 | 实现情况 | 对应代码 |
|--------|----------|----------|
| 双向（异常检测 + 建议调整） | ✅ | `weather.ts` 同时实现 `detectWeatherAnomalies` + `generateWeatherAdjustments` |
| 趋势分析 | ✅ | `seasonal-data.ts` 的 `detectPriceAnomalies` 对比上月价格计算变化率 |
| 四级降级数据获取 | ✅ | `weather.ts` 的 `getWeather()` 函数：API→缓存→推算→默认 |
| 自动周报 | ⬜ | 未实现（可扩展，记录一周天气+价格趋势生成摘要） |

---

## 七、演示截图说明

1. **聊天页侧边栏天气卡片**：显示实时天气（温度/湿度/风速）、数据来源标签（实时/缓存/估算）
2. **高温异常预警**：气温 ≥ 32°C 时显示预警区域和烹饪建议
3. **季节食材推荐**：左侧面板显示当季食材价格、推荐分类标签
4. **价格异常提醒**：食材价格较上月波动 > 30%（涨）或 < -25%（跌）时显示彩色预警

（截图见附件）
